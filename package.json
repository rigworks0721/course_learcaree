{
  "name": "lclp",
  "version": "1.0.0",
  "scripts": {
    "dev": "browser-sync start --server --files \"**/*.html, **/*.css, **/*.js\"",
    "build": "powershell -Command \"Remove-Item dist -Recurse -Force -ErrorAction Ignore; New-Item -ItemType Directory dist; Copy-Item coaching,coffee,kampo,personalcolor,images,index.html,style.css dist -Recurse\""
  },
  "devDependencies": {
    "browser-sync": "^3.0.4"
  }
}